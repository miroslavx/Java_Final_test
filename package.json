{
  "name": "pitsa-e2e-tests",
  "version": "1.0.0",
  "description": "E2E tests for the Pitsa web application using Playwright",
  "scripts": {
    "test": "npx playwright test",
    "e2e": "npx playwright test",
    "e2e-ui": "npx playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.39.0"
  }
}